# Repository Agent Instructions

## 项目与文档

本仓库开发 Verified Coding Agent Runtime：在项目规范约束下执行编码任务，并以与代码版本关联的证据决定完成状态。

- [CODING_AGENT_DEVELOPMENT_SPEC.md](CODING_AGENT_DEVELOPMENT_SPEC.md)：产品要求、运行行为和验收标准。
- [DEV_PLAN.md](DEV_PLAN.md)：12 阶段实施顺序、交付物、待统一事项和开发进度。
- 本文件：维护这个仓库时的工作方式，不代替产品规格。

首次工作先读规格、开发计划及与任务相关的代码；后续按变更重读相关章节。区分已经实现的能力、计划中的能力和未经验证的推测。当前实现状态以 DEV_PLAN.md 和实际文件为准。

用户当前明确指令优先于本文件中的默认流程。遇到规格与计划冲突，记录冲突并统一受影响的要求，不能默默选择更宽松的完成标准。

## 范围与推进

1. 先检查当前文件和用户已有修改，确定任务边界；用 `rg` 查找代码和调用位置。
2. 按 DEV_PLAN.md 的依赖顺序实现。默认首次仅 P01；用户授权多个阶段时，满足阶段验收后在授权范围内连续推进。
3. 不将规划或评审请求解释为实施全部项目；不把产品中的人工审批步骤机械套用成仓库每次编辑都要再次询问。
4. 日常可逆实现选择自行处理；只针对无法从上下文确定、且影响正确性或授权范围的问题请求澄清。
5. 更新当前阶段清单和实际开发记录，保留失败、未验证项与下一步，不提前将阶段标为 DONE。

## 实现原则

- Python 3.12+；使用类型标注与 Pydantic v2 表达需要校验的领域结构。
- 源码逐步放入 `src/coding_agent/`；按当前需求建立文件，不预建全部未来子系统。
- 优先复用现有代码、标准库和已安装依赖。保留规格明确要求的边界与接口，不增加无实际使用者的工厂、插件系统或通用框架。
- V1 串行执行、一个真实 Provider；不引入 Agent 团队、Web、多用户、远程执行、复杂 RAG 或自动 PR。
- 复杂度评估复用 Explorer/Planner 与既有重规划流程；在 P07 增加计划元数据，不单独建立分类 Agent 或评分服务。
- CLI/API 只调用应用流程；领域规则不依赖 CLI、模型供应商或操作系统执行细节。
- 产品中的 Agent 返回结构化结果；所有 shell、文件修改和 Git 副作用经过 Tool Runtime。进程执行优先使用参数列表和显式工作目录。
- Workspace、事件、状态和验证结果分工明确；避免重复维护独立可写的同一事实。

## 必须保持的运行约束

本节约束所开发的产品运行时。维护本仓库时使用当前环境已授权的开发工具，不要求先有可用的产品 Tool Runtime 才能编写它。

- 只有 Workflow Engine 改变任务状态；Coder 不能自行标记 VERIFIED。
- QualityGate 检查必需验收证据及适用 Review。缺失、失败、跳过、不可用、不确定或过期的必需证据均不算通过。
- Evidence 关联 criterion/check ID、计划版本、项目知识修订和实际代码快照；Git HEAD 不能代表所有未提交改动。
- init 复用已有文档，区分事实、明确规则和假设；刷新保留用户规则，任务上下文引用相关来源。
- 新计划保留旧版本、变更原因和适用授权；不能通过删除失败条件或扩大权限取得成功。
- 复杂度与风险分别记录；按范围、耦合、不确定性和验证难度判断，不能仅按代码行数或文件数分类。低复杂度不能绕过高风险检查。
- 大型重构先明确行为不变量与基线，逐批实现并验证；计划持续保留后续必需范围，当前批次完成不能代表整个需求完成。
- 新依赖、范围扩大或验证困难触发复杂度重评估与计划版本更新；跨批次保留已有授权边界和预算消耗，不默默增加资源或重置重试次数。
- 实际动作、退出码、Diff 和验证结果由运行时记录。模型总结不能代替执行事实，也不能写入权威成功记录。
- 先持久记录请求，再执行副作用；记录不可用时停止新副作用。请求有记录而结果缺失时，先检查真实状态再恢复。
- shell/test 命令可能执行任意仓库代码；命令前缀过滤和 Git Worktree 都不能作为进程/网络隔离保证。后端无法强制所请求权限时拒绝执行并说明原因。
- 恢复和清理必须保留用户修改；日志、检查点等控制数据不能被 Agent 工具直接改写。

## 验证

基础检查由 P01 配置。配置未建立时应报告“尚未配置”，不要假装命令已经可用或已通过。

在项目虚拟环境中执行：

```bash
python -m pytest
python -m ruff check .
python -m ruff format --check .
python -m mypy src/coding_agent
```

- 先运行受影响的检查，再完成当前阶段要求的完整验证；仅在新问题或改动需要时扩大或重复测试。
- 每个领域模块及关键状态、图、权限、门禁、证据和恢复逻辑必须有测试；测试验证外部行为与不变量，不照抄实现。
- 文档及简单低影响修改使用相应格式/差异检查，不机械新增测试或运行依赖真实模型的流程。
- 模型行为通过 Fake/模拟响应离线覆盖；真实 API 冒烟单独执行和记录，不将缺少凭据当作通过。
- 文件、进程与 Git 故障测试使用临时目录/仓库；不污染全局配置或破坏用户项目。
- 检查失败需区分当前改动、原有问题、环境限制和未知原因。不得删除测试、弱化断言或修改验收目标来掩盖失败。

## 文件、Git 与外部副作用

- 保留用户改动；不自动 reset/clean 整个工作区，不删除归属不明的文件。
- Windows 下删除或移动前核实绝对路径与目标范围，使用同一 shell 的原生命令；后台进程使用隐藏窗口。
- 凭据通过适当的环境或配置注入；不写入源码、开发记录、事件或结果工件。
- 按逻辑单元整理 Diff，提交、push、发布和对外消息遵循当前会话授权；已有授权无需重复索取。

## 文档与交付

- 修改公共模型、CLI、模块关系或完成规则时，同步受影响的规格和开发计划。
- 正常沟通使用中文；代码标识符和公共 API 名称使用英文，保持现有文档语言风格。
- 交付说明包含：实际改动、检查及结果、未完成/未验证项、下一步。引用真实文件或工件，不提供推测性通过结果。
- 竞品能力引用官方资料并记录核对范围；本项目的设计承诺和实测优势分别表述。没有对照数据时不得声称更可靠或更省成本。
